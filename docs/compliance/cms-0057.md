# CMS-0057-F Compliance (Provider Directory)

CMS-0057-F ("Advancing Interoperability and Improving Prior Authorization Processes") requires payers, and by extension credentialing organizations, to publish provider directory information via FHIR R4 APIs.

## Platform coverage

The platform exposes a **FHIR R4 Practitioner** endpoint at `/api/fhir/Practitioner` with:

- Search with standard FHIR parameters (`identifier`, `family`, `given`, `active`, `_lastUpdated`, `_count`, `_offset`).
- Read (`GET /Practitioner/{id}`).
- CapabilityStatement at `/api/fhir/metadata`.
- API-key authentication; errors as `OperationOutcome`.
- Rate limiting and full audit logging.

See [`docs/api/fhir.md`](../api/fhir.md) for the full reference.

## Data included

For each approved provider:

- NPI (`identifier` with system `http://hl7.org/fhir/sid/us-npi`).
- Internal platform id (`identifier` with system `http://essenhealthcare.com/providerId`).
- Official name (legal).
- Work email and business phone (telecom).
- Active licenses with issuer and expiration (`qualification`).
- Board certifications (`qualification`).
- `active` flag reflects current credentialed status.

## Data excluded

PHI is never published through FHIR:

- No SSN
- No date of birth
- No home address
- No home phone
- No document content

## Currency

- Updates propagate within 1 minute of staff changes.
- `_lastUpdated` filter supports incremental sync.
- Full refresh pulls are supported but rate-limited; use incremental sync where possible.

## Roadmap

If required by regulation or contract:

- `Organization` resource for Essen-as-entity and facilities.
- `HealthcareService` for service locations.
- `Location` for physical addresses.
- `PractitionerRole` for per-location, per-specialty bindings.

These would be added as additional endpoints; no changes are planned to the existing `Practitioner` shape.

## Conformance testing

We run automated contract tests against the FHIR endpoint using the NCQA-published test vectors and HL7's Inferno validator. Failures block merge.

See [`docs/dev/testing.md`](../dev/testing.md).

## Sample directories

Essen can provide a non-live sample directory on request for onboarding integrations. Contact the Integration Manager.
